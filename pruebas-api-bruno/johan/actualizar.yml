info:
  name: actualizar
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/api/subjects/1
  body:
    type: json
    data: |-
      {
        "name": "Servicios Web",
        "practiceHours": 6,
        "abbreviation": "SW-G1",
        "rotates": false,
        "isActive": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
